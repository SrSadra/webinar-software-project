export enum Roles {
    USER = "user",
    DOCTOR = "doctor",
    MANAGER = "manager"
}

export enum UserRoles {
    USER = "user",
    DOCTOR = "doctor"
}

export enum ManagerRoles {
    SUPERMANAGER = "supermanager",
    MANAGER = "manager"
}